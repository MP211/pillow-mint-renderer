# pillow-mint-renderer

*See dev CLI for example use.*

Time to process has the following (configurable) factors:

- output dimensions
- FPS
- animation duration
- encoder quality
- optional dithering
- palette derivation (internal)
